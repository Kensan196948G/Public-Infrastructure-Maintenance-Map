{
  "name": "@pimm/source-adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Per-source adapters and sample datasets (fictitious, Japan-domain sample data only)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./registry": "./src/registry.ts",
    "./worker-adapters": "./src/worker-adapters.ts",
    "./worker-http": "./src/worker-http.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pimm/contracts": "workspace:*",
    "@pimm/database": "workspace:*",
    "@pimm/ingestion-core": "workspace:*",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.0"
  }
}
