# 本番ビルドの API 接続先(公開値・secretではない)。
# Vite は .env.production を .env.local より優先するため、検証用 .env.local が
# 本番バンドルへ混入する事故(2026-07-30 障害・DL-014)を構造的に防ぐ。
VITE_API_BASE_URL=https://api.pimm.mirai-dx-platform.com/api/v1
