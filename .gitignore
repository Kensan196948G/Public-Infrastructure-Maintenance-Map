# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo
.wrangler/

# Environment — never commit real secrets
.env
.env.*
!.env.example
# 公開値のみ(本番 API base)。検証用 .env.local の本番ビルド混入を防ぐ宣言的ガード (DL-014)
!apps/web/.env.production

# Test artifacts
coverage/
playwright-report/
test-results/

# ClaudeOS runtime state (machine-local, not source of truth)
state.json
reports/
.claude/settings.local.json

# Agent session artifacts (plans, evidence, local config)
.omo/
.opencode/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
